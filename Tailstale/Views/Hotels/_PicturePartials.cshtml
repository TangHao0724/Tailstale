@model business
@if (Model.photo_url == null){
    <img style="width:200px;height:120px" src="~/Images/NoImg.jpg" title="尚無圖片" />

}
else
{
    <img style="width:200px;height:120px" src="~/@Model.photo_url" title="@Html.DisplayFor(model => model.name)" />
}

