@model IEnumerable<Tailstale.MedRecordDTO.HospDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>住院記錄</h1>
<span>
    <a asp-controller="medical_record" asp-action="Index">病歷記錄</a>
    <a asp-controller="nursing_record" asp-action="Index">護理記錄</a>
</span>
<br />
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.admission_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.discharge_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.medical_record_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nursing_record_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ward_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.memo)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @* 顯示：入院時間，超連結：Details *@
                    <a href="Hosp/Details/@item.id">@item.admission_date</a>
                </td>
                <td>
                    @Html.DisplayFor(moitemdelItem => item.discharge_date)
                </td>
                <td>
                    @*住院的看診紀錄*@
                    <a href="/medical_record/Details/@item.medical_record_id">@item.created_at</a>
                </td>
                <td>
                    @*住院的護理紀錄*@
                    <a href="/nursing_record/Details/@item.id">詳細記錄</a>
                <td>
                        @Html.DisplayFor(modelItem => item.ward_id)
                </td>
                <td>
                        @Html.DisplayFor(modelItem => item.memo)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.id">修改</a>
                </td>
            </tr>
        }
    </tbody>
</table>
