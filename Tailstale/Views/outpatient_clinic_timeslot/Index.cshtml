@model IEnumerable<Tailstale.Models.outpatient_clinic_timeslot>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.outpatient_clinic_timeslot_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.startat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.endat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.outpatient_clinic_timeslot_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.startat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.endat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.outpatient_clinic_timeslot_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.outpatient_clinic_timeslot_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.outpatient_clinic_timeslot_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
