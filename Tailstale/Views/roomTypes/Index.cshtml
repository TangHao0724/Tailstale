@model IEnumerable<Tailstale.Models.roomType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.roomType1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FK_business)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.roomType1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FK_business.name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.roomType_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.roomType_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.roomType_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
